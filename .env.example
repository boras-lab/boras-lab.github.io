# Copy to .env.local for local overrides (gitignored).

# In development leave this empty — the Vite dev server proxies /api to the
# Django backend (see vite.config.js). In production, set it to the API origin,
# e.g. https://api.taskflow.example.com
VITE_API_BASE=
